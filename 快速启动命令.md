# FindAI 快速启动

## 首次安装

在 Windows PowerShell 中运行：

```powershell
Set-Location 'E:\AI\Project\FindAI'
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
```

## 日常启动

```powershell
Set-Location 'E:\AI\Project\FindAI'
.\.venv\Scripts\findai.exe
```

推荐使用 `findai.exe` 启动。它是当前 Python 虚拟环境中的启动器，不是可复制到其他电脑独立运行的单文件 EXE。

开发调试时也可以使用：

```powershell
.\.venv\Scripts\python.exe -m findai.main
```

停止程序时，在运行窗口按 `Ctrl+C`。

## 使用方式

1. 浏览器打开 [http://127.0.0.1:7070](http://127.0.0.1:7070)。
2. 确认页面中的扫描网段和端口，然后点击扫描按钮。
3. 扫描完成后，在服务列表中查看发现的模型服务和模型。
4. 上层系统接入时，将 OpenAI Base URL 设置为：

```text
http://运行FindAI的主机:7070/v1
```

本机调用时可使用：

```text
http://127.0.0.1:7070/v1
```

获取可用模型列表：

```powershell
Invoke-RestMethod -Uri 'http://127.0.0.1:7070/v1/models'
```

启动配置默认从项目根目录的 `.env` 读取。
