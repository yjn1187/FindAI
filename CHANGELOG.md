# Changelog

## Unreleased

### 修复

- 扫描期间新发现的服务通过自适应队列逐个接续展示，并为新增卡片提供进入动画。
- 鉴权模型服务可从浏览器保存的常用密钥中选择并填充，同时区分“仅保存”和“用于 FindAI 访问”。

## 1.0.0 - 2026-08-10

FindAI 1.0 提供局域网模型服务发现、持久化服务注册表和统一的 OpenAI 兼容网关。

### 主要能力

- 扫描并识别 OpenAI 兼容服务与 Ollama 服务。
- 聚合在线模型，并通过稳定的路由模型 ID 转发请求。
- 支持普通 JSON 请求、SSE 流式透传和 Ollama Chat Completions 适配。
- 提供中文管理控制台、手工扫描、服务复检、清空结果和多密钥管理。
- 支持显式放行可信公网单主机，并默认采用手工扫描模式。

### 后续优化

- 为扫描结果列表增加分批接续动画，改善高速扫描时结果瞬间出现、缺少连续动态感的问题。
- 为需要鉴权的已发现模型支持从常用密钥中选择并快速填充。
